<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coursework1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coursework1">
        <activity android:name=".AdvancedLevelActivity"
            android:label="Advanced Level"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".IdentifyCarActivity"
            android:label="Identify the Car"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HintActivity"
            android:label="Hints"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SecondActivity"
            android:label="Identify the Car Make"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>